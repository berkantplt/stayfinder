<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- PHP varsayılanı 128M; suite görsel işleyen import testleriyle
             (imagecreatefromstring) bu sınırın kıyısında koşuyordu ve yeni test
             eklendikçe rastgele "memory exhausted" veriyordu. Sınır testin
             kendisiyle ilgili değil, tüm koşunun toplam kullanımıyla. -->
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <!-- Hermetik testler: gözden kaçan gerçek API çağrısı sessizce para
             harcamak yerine anında ve tutarlı şekilde patlasın -->
        <env name="OPENAI_API_KEY" value="sk-test-suite-fake-key"/>
        <env name="FIRECRAWL_API_KEY" value=""/>
        <env name="JINA_API_KEY" value=""/>
        <!-- Re-ranker testlerde varsayılan kapalı (fake yanıt dizilerini kaydırmasın);
             kendi testi config ile açar -->
        <env name="AI_RERANK" value="false"/>
        <!-- Sohbet asistanı canlıda DONDURULDU; kod korunduğu için testleri
             açık koşar (dondurma davranışının kendi testi bayrağı kapatır) -->
        <env name="AI_CHAT_ENABLED" value="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
